# 📊 Strict Coverage Configuration
# 
# Silicon Valley Standard: 100% coverage objetivo para Domain y Application
# 90% para Infrastructure
# 80% para Interface

[run]
source = app
branch = True  # Branch coverage (if/else paths)
parallel = True
concurrency = 
    thread
    multiprocessing

# Omitir archivos que no necesitan cobertura completa
omit = 
    */__pycache__/*
    */tests/*
    */venv/*
    */migrations/*
    */static/*
    */templates/*
    app/config.py  # Config es difícil de testear 100%
    app/diagnostics.py
    app/maintenance.py
    app/version.py

[report]
# Fallar si cobertura es menor a estos umbrales
fail_under = 80

# Mostrar líneas faltantes
show_missing = True
skip_covered = False

# Precisión
precision = 2

# Excluir líneas que no necesitan cobertura
exclude_lines =
    # Líneas que no requieren testing
    pragma: no cover
    def __repr__
    def __str__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    pass
    \.\.\.$  # Elipsis
    
    # Logging
    logger\.
    logging\.
    
    # Excepciones específicas
    except ImportError:
    except KeyError:
    except Exception as e:

# Sorting
sort = Cover

[html]
directory = htmlcov
title = "Jorge Aguirre Flores - Coverage Report"

[xml]
output = coverage.xml
