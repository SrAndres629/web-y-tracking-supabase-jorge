import requests
import json

# USING THE API TOKEN (NOT GLOBAL KEY)
API_TOKEN = "<NEED_THE_TOKEN_STRING>" 
# Wait, I don't have the token string! I only have the ID.
# Token strings are shown only once upon creation.

if __name__ == "__main__":
    print("Wait, I can't test the token without the token string.")
