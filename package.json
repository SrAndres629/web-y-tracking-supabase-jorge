{
    "name": "jorge-aguirre-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd static && npm install",
        "build": "cd static && npm run build"
    },
    "engines": {
        "node": ">=18"
    }
}